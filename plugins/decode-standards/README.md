# decode-standards

DECODE engineering standards and conventions, delivered to Claude Code sessions
as a skill plus a review command.

## Contents

| Path | Purpose |
| --- | --- |
| `skills/decode-standards/SKILL.md` | The standards themselves. Loaded when Claude needs the house rules. |
| `commands/decode-standards-check.md` | `/decode-standards-check`, reviews the working tree against those rules. |

## Status

The skill is currently a stub with TODO placeholders. Fill it in with DECODE's
real conventions and bump `version` in both `.claude-plugin/plugin.json` and the
matching entry in the marketplace manifest.
